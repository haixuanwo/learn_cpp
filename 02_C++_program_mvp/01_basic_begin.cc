
#include <iostream>

/**
 * @brief
 *
 * 1、类型
 * （1）变量
 * bool, char, wchar_t, char16_t, chart32_t, short, int, lone, long long
 * float, double, long double
 *
 * （2）转换
 * 向上转换
 * 有符号向无符号转换
 * 隐式转换与显示转换
 * static_cast、reinterpret_cast、const_cast和dynamic_cast
 *
 * 2、指针、应用、std::move
 *
 * 3、字符串、数组、结构体
 *
 * 4、语句
 * if, switch case, for, while, do while, break, continue, goto,
 * try throw catch what
 *
 * 5、全局变量、局部变量、命名空间
 *
 * 6、static、const
 *
 */


